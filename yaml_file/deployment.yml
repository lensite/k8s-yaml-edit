apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: tomcat
  name: tomcat
  namespace: tomcat
spec:
  replicas: 10
  selector:
    matchLabels:
      app: tomcat
  template:
    metadata:
      labels:
        app: tomcat
    spec:
      containers:
      - image: registry-vpc.cn-beijing.aliyuncs.com/wuu-docker/independent-gzt:v0.2
        name: redis
        ports:
        - containerPort: 443
